# Design

This directory records framework-model decisions. A declaration is the single
source the framework reads — for routing, for validation, and for help — so a
decision here is usually about what a command author must state and what the
framework may infer.
