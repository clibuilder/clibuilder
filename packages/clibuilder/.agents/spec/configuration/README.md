---
spec-type: behavioral
---

# Configuration

Governs `ts/config.ts` and `ts/find_up.ts` — locating a configuration file by
the supported filename conventions, reading its format, and validating it
against the schema the invoked command declared.

## Use Cases

<!-- stub: authored in the per-unit explore grill -->
