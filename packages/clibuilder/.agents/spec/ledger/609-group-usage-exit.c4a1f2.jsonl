{"kind": "leash", "cr": "609-group-usage-exit", "leash": "auto-spec", "by": "derived", "approach": ["a command without run invoked bare is a usage error, not a success", "the base command declares no run, so an application with no default command is a group too", "one rewritten scenario on each of two frozen suites, cleared by the user in-session"]}
{"kind": "gate", "cr": "609-group-usage-exit", "gate": "spec", "verdict": "approve", "by": "unional", "cause": "clearance", "frozen": ["builtin-commands/builtin-commands.feature", "execution/execution.feature"], "why": {"floor": "narrowing — classify-edit-class reports MIXED on both suites: 'running the base command itself shows help' becomes 'the base command declares no run of its own', and 'a group command with nothing to run shows help' becomes 'a group command invoked bare shows help and exits with the usage code'", "clearance": "the user chose, in-session on PR #612, to amend the frozen spec rather than restore the base command's run", "blast": "low — two nodes, two scenarios rewritten, check-suite passes on both"}}
