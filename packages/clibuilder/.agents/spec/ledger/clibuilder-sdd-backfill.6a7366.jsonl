{"kind":"leash","cr":"clibuilder-sdd-backfill","leash":"auto-spec","by":"user","approach":["backfill the whole clibuilder surface as its own mission, chosen by the Council over a scoped root-plus-touched-nodes cut","capability-first layout, ten nodes, colocated per the args-minus precedent","partition-quality measured and declined to decide; layout follows the recommendation, not the numbers","the zod removal that prompted this is deferred to a later mission"]}
{"kind":"followup","cr":"clibuilder-sdd-backfill","class":"backlog","summary":"replace zod with Standard Schema for configuration plus an in-house type descriptor for arguments and options, and stop re-exporting z from the public entry point","why":"the framework pins every consumer to its own validator major, and the validator is its heaviest runtime dependency on a package that optimizes startup"}
{"kind": "followup", "cr": "clibuilder-sdd-backfill", "class": "defect", "node": "command-definition", "summary": "an option's `default` is not type-checked against its declared `type`", "why": "cli.Command.Options is Record<Name, Options.Entry<Type>> with Type defaulting to z.ZodType<any> and never inferred per entry, so Entry's `default?: z.infer<Type>` widens to any. `{ type: z.array(z.string()), default: 5 }` compiles and the option still types as string[], so run receives a value its own declaration says is impossible. Closing it needs per-entry inference of Type; verified against ts/cli.ts during the command-definition backfill."}
{"kind": "followup", "cr": "clibuilder-sdd-backfill", "class": "defect", "node": "input-parsing", "summary": "the `--` terminator remainder never reaches the run arguments, and using `--` emits a spurious invalid-key error", "why": "fillInputOptions skips the `_` key but not `__`, so `__` falls through to lookupOptions, matches no declared option, and pushes {type:'invalid-key',key:'__'}. lookupCommand.Result declares an `__?: string[]` field that is therefore unreachable. Verified: parseArgv(['node','cli','--','abc']) yields {_:[],__:['abc']}, and lookupCommand on a command with no options returns errors [{invalid-key,__}] and args {_:[]}."}
{"kind": "followup", "cr": "clibuilder-sdd-backfill", "class": "defect", "node": "input-parsing", "summary": "`--` arriving while an option awaits values is consumed as that option's value instead of opening the terminator", "why": "isOption('--') is false (no word character), so the reducer's option-state branch claims the token before the `v === '--'` branch is reached. Verified: parseArgv(['node','cli','--opt','--','abc']) yields {_:[],opt:['--','abc']} with no __ at all."}
{"kind": "followup", "cr": "clibuilder-sdd-backfill", "class": "defect", "node": "input-parsing", "summary": "a negative number on the command line is tokenized as an option", "why": "isOption('-5') is true because the token starts with '-' and /\\w+/ matches its digits, so startSingleCharacterOptions opens an option named '5'. A command declaring a number argument or option cannot be given a negative value. Verified: parseArgv(['node','cli','-5']) yields {'5':['true'],_:[]}."}
